package __TOP_LEVEL_PACKAGE__.gwt.place;

import com.google.gwt.app.place.Place;

/**
 * Base type of places for an app.
 */
public abstract class ApplicationPlace extends Place {
  // TODO is there actual value in this base class? Yes, if visitors
  // and filters leave the keys and come back to places.
}
