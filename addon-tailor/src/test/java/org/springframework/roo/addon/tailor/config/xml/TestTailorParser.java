package org.springframework.roo.addon.tailor.config.xml;

import org.junit.Test;
import org.springframework.roo.addon.tailor.actions.Focus;

/**
 * Tests for {@link Focus}
 * 
 * @author Vladimir Tihomirov
 */
public class TestTailorParser {

    @Test
    public void testStandard() {
        // Document readXml = XmlUtils.readXml(this.getClass()
        // .getResourceAsStream("testtailor.xml"));
        // Element root = readXml.getDocumentElement();
        // List<TailorConfiguration> configs = TailorParser
        // .mapXmlToTailorConfiguration(root);
        // Assert.assertNotNull(configs);
        // Assert.assertNotNull(configs.get(0));
        // Assert.assertNotNull(configs.get(1));
        // Assert.assertNotNull(configs.get(2));
        // Assert.assertEquals("afpj", configs.get(0).getName());
        // Assert.assertEquals("test", configs.get(1).getName());
        // Assert.assertEquals("helper", configs.get(2).getName());
        // Assert.assertEquals(3, configs.get(0).getCommandConfigs().size());
        // Assert.assertEquals(2, configs.get(1).getCommandConfigs().size());
        // Assert.assertEquals(1, configs.get(2).getCommandConfigs().size());
    }

}
